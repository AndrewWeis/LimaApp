<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/MW"
    android:layout_marginHorizontal="16dp">

    <ImageView
        android:id="@+id/choice_image"
        style="@style/W24_H24"
        app:layout_constraintBottom_toBottomOf="@+id/choice_text"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/choice_text"
        tools:srcCompat="@drawable/ic_priority_fire_1" />

    <TextView
        android:id="@+id/choice_text"
        style="@style/RobotoRegular.Black.Size14.Zero_W"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/selected_image"
        app:layout_constraintStart_toEndOf="@id/choice_image"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Priority item" />

    <ImageView
        android:id="@+id/selected_image"
        style="@style/W24_H24"
        android:src="@drawable/ic_done"
        app:layout_constraintBottom_toBottomOf="@+id/choice_text"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/choice_text"
        app:tint="@color/secondaryColor" />

    <View
        android:id="@+id/view"
        style="@style/M_H1"
        android:layout_marginTop="16dp"
        android:background="@color/lightGray"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/choice_text" />

</androidx.constraintlayout.widget.ConstraintLayout>
