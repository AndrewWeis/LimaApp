<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/tasksFragment">

    <fragment
        android:id="@+id/tasksFragment"
        android:name="start.up.tracker.ui.tasks.TasksFragment"
        android:label="Home"
        tools:layout="@layout/fragment_tasks" />
    <fragment
        android:id="@+id/addTaskFragment"
        android:name="start.up.tracker.ui.AddTaskFragment"
        android:label="Add task"
        tools:layout="@layout/fragment_add_task" />
    <fragment
        android:id="@+id/analyticsFragment"
        android:name="start.up.tracker.ui.AnalyticsFragment"
        android:label="Analytics"
        tools:layout="@layout/fragment_analytics" />
    <fragment
        android:id="@+id/categoryFragment"
        android:name="start.up.tracker.ui.CategoryFragment"
        android:label="Categories"
        tools:layout="@layout/fragment_categories" />
    <fragment
        android:id="@+id/settingsFragment"
        android:name="start.up.tracker.ui.SettingsFragment"
        android:label="Settings"
        tools:layout="@layout/fragment_settings" />
</navigation>